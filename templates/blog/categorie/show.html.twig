{% extends 'base.html.twig' %}

{% block title %}{{ categorie.name }}{% endblock %}

{% block body %}
    <main class="container">
        <div class="row g-5">
            <div class="col">
                <h3 class="pb-4 mb-4 fst-italic border-bottom">{{ categorie.name }}</h3>
            </div>
            <div id="article-list">
                {% include 'blog/produit/list.html.twig' with {articles: categorie.articles, leftCol:4, rightCol:8 } %}
            </div>
        </div>
    </main>
{% endblock %}
