{% set catalogue = catalogue_services.findAllCategorie.findAll %}
{% set catalogue_count = catalogue_services.findAllCategorie.countCaategories %}
{% set i = 1 %}
<div class="top-header-area" id="sticker">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 col-sm-12 text-center">
                <div class="main-menu-wrap">
                    <!-- logo -->
                    <div class="site-logo">
                        <a href="#">
                            <img src="{{ asset('assets/portail/assets/img/Logo Agridevaf_thumbnail.png') }}" alt="">
                        </a>
                    </div>
                    <!-- logo -->

                    <!-- menu start -->
                    <nav class="main-menu">
                        <ul>
                            <li class="current-list-item"><a href="{{ path('app_portail') }}">Accueil</a></li>
                            <li><a href="#">A propos de </a></li>
                            <li><a href="#">Les produits AGROMAX</a>
                                <ul class="sub-menu">
                                    {% for element in  catalogue %}
                                        <li><a href="#">  {{element.libelle}}</a></li>
                                    {% endfor %}
                                </ul>
                            </li>
                            <li><a href="#">Actualités</a></li>
                            <li><a href="#">Nos contacts</a></li>
                            <li><a href="{{ path('boutique') }}">La boutique</a></li>
                            <li>
                                <div class="header-icons">
                                    <a class="shopping-cart" href="#"><i class="fas fa-shopping-cart"></i></a>
                                    {#<a class="mobile-hide search-bar-icon" href="#"><i class="fas fa-search"></i></a>#}
                                    <a class="" title="mon compte" href="#"><i class="fas fa-user"></i> {{ app.session.get('CURRENT_CLIENT_SESSION') }}
                                        {#<br><div class="badge bg-dark text-light"></div>#} </a>
                                    <ul class="sub-menu">
                                        <li><a href="{{ path('app_client') }}">mon compte</a></li>
                                        <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
                                    </ul>
                                </div>
                            </li>
                        </ul>
                    </nav>
                    <a class="mobile-show search-bar-icon" href="#"><i class="fas fa-search"></i></a>
                    <div class="mobile-menu"></div>
                    <!-- menu end -->
                </div>
            </div>
        </div>
    </div>
</div>